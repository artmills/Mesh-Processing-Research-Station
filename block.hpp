#pragma once

/** Representing a single block in the terrain. 
 *
 * This list should be expanded to include new block types. */
enum class Biome
{
	UNDEFINED = -1,
	Grassland = 0,
	Mountain = 1,
	Ocean = 2,
	NUMBER_OF_BIOMES
};
